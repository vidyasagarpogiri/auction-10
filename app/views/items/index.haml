%h1 Items Being Auctioned: 

- @items.each do |item| 
	%ul
		%li.list_of_items
			= link_to item.name, item_path(item.id) 
		%li= link_to 'Delete', item, method: :delete, data: { confirm: 'Are you sure?' } 

